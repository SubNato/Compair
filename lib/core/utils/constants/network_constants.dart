abstract class NetworkConstants {
  const NetworkConstants();

  static const baseUrl = //Place IP ADDRESS HERE //Using IP Address so that Android devices work. IOS will work regardless.
  static const authority = //Place IP ADDRESS HERE;
  static const adminUrl = //Place IP ADDRESS HERE;
  static const apiUrl = //Place route add-on here;
  static const headers = {'Content-Type': 'application/json; charset=UTF-8'};
  static const pageSize = 10;
}
